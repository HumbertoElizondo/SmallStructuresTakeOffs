@model IEnumerable<SmallStructuresTakeOffs.Models.Project>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProjectCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProjectName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProjectCode)
                @*<a asp-action="Details" asp-route-id="@item.ProjectId">@Html.DisplayFor(modelItem => item.ProjectCode)</a>*@
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProjectName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProjectId">Edit</a> |
                <a asp-action="Index" asp-controller="CatchBasins"  asp-route-id="@item.ProjectId">Catch Basins</a> |
                <a asp-action="Index" asp-controller="HeadWalls" asp-route-id="@item.ProjectId">Headwalls</a> |
                <a asp-action="Delete" asp-route-id="@item.ProjectId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
